# Project Agent Instructions

- After completing and validating any file changes, commit the intended changes and push them to the configured GitHub remote in the same task.
- Before committing, inspect `git status`, `git diff`, and recent commits. Stage only files related to the task.
- Never push partial, unvalidated, or unrelated changes.
